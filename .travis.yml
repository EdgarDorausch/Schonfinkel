language : node_js
node_js :
 - stable
install:
 - npm install
script:
 - echo "Running tests against $(node -v)..."
 - npm test

jobs:
  include:
    - stage: Produce Coverage
      node_js: node
      script: npm coverage | ./node_modules/coveralls/bin/coveralls.js
